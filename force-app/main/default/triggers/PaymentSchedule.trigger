trigger PaymentSchedule on Payment_Schedule__c (
    before insert, 
    before update, 
    before delete, 
    after insert, 
    after update, 
    after delete, 
    after undelete) {

//** Deprecate
}